#include <iostream>

class a {
public:
     add(int a, int b) {
         a + b;
    }

     add(float a, float b) {
         a + b;
    }

     subtract(int a, int b) {
         a - b;
    }

     subtract(float a, float b) {
         a - b;
    }
};

int main() {
     a a1,b2,c,d;

      a1= operations.add(1, 2);
    cout << "addition: " << a1 <<endl;

     b2 = operations.add(5.5, 4.3);
    cout << " addition: " << b2 <<endl;

    
     c = operations.subtract(10, 15);
    cout << " subtract: " << c <<endl;

    
     d = operations.subtract(4.2, 1.2);
    cout << "subtract: " << d << endl;

    
}

